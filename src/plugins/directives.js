// import utils from '../assets/scripts/utils'
const install = function (Vue, options) {
  // Vue.directive('focus', {
  //     inserted(el){
  //         el.focus()
  //     }
  // })
}

export default {
  install
}
